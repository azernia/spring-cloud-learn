# 引导文件
spring:
  application:
    name: user-nacos-service
  # 选择 nacos 中的环境
  profiles:
    active: dev # 环境
  cloud:
    nacos:
      config:
        file-extension: yaml
        server-addr: localhost:8848
#        server-addr: localhost:80
      discovery:
        server-addr: localhost:8848
#        server-addr: localhost:80
